{
  "name": "pivotal_prepare_commit",
  "version": "1.0.0",
  "description": "A simple git hook script that pre-populates your commit messages with the details of a story started in Pivotal Tracker.",
  "main": "./dist/prepare-commit-msg.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build" : "babel ./src --presets babel-preset-es2015 --out-dir ./dist",
    "prepublish": "npm run build"
  },
  "author": "Filype Pereira <pereira.filiype@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "request": "^2.83.0"
  },
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-preset-es2015": "^6.5.0"
  }
}
